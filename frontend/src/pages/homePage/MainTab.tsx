import { Box } from "@mui/material";
import RamadView from "./ramadView/RamadView";

const MainTab = () => {
  return (
    <Box sx={{height: '100%'}}>
      <RamadView></RamadView>
    </Box>
  );
};


export default MainTab;